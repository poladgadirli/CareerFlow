spring:
  application:
    name: careerflow

  datasource:
    url: jdbc:postgresql://localhost:5432/careerflow_db
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080